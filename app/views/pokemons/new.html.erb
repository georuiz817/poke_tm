<h1> Add a new Pokemon to your group: </h1> 

<%= form_for @pokemon do |f| %>
        <%= f.label :name, "Name" %>
        <%= f.text_field :name%>
        <%= f.label :tm_id, "Link the desired TM to this pokemon:" %>
        <%= f.collection_select :tm_id, Tm.all, :id, :name%> 
    <%= f.submit "Submit" %>
        <% end  %> 
        <br>

        

        # a user should be able to create a TM and it will then be linked to the requested pokemon
<%= link_to "Return Home", root_path %>
</body>
#####
